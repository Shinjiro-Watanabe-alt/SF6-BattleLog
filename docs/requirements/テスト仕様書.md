# SF6-BattleLog テスト仕様書

## 1. テスト方針

### 1.1 構成

- **静的・ロジック確認**: JavaScript構文、入力制約、JSONデータ構造、IndexedDB移行を確認する。
- **統合・実機確認**: AndroidブラウザまたはPWAで、実際の入力、保存、再読込、取込、外部遷移を確認する。

### 1.2 外部サービスの扱い

- GitHub Pagesは手動公開後にページを確認する。
- YouTubeは限定公開URLを実際に開く確認のみ行う。動画の内容やアカウント情報はテストデータに含めない。

### 1.3 不安定な確認の扱い

通信・YouTube・GitHub Pagesの一時的な失敗は、まず再実行または再読込する。再現する場合は、URL、端末、操作、表示されたエラーを記録して原因を調査する。

## 2. 実行方法

```bash
node --check app.js
node --check sw.js
```

リポジトリ直下で実行する。ブラウザテストはGitHub Pages公開後にAndroid実機で行う。

## 3. テストケース一覧

### 3.1 静的確認: `app.js` / `sw.js`（PWAロジック、2件）

要件定義書の5〜7章に対応。

| # | テストケース | 入力／前提 | 期待結果 |
|---|---|---|---|
| 1 | JavaScript構文確認 | `node --check app.js` | エラーなし |
| 2 | Service Worker構文確認 | `node --check sw.js` | エラーなし |

### 3.2 実機統合確認: 動画中心のホーム・セット記録（11件）

| # | テストケース | 確認内容 |
|---|---|---|
| 1 | セット手入力 | 必須項目入力後、ダッシュボードと最近の対戦に反映される |
| 2 | 最大入力数 | ダメージ源・被ダメージ源・目立った出来事が各上限を超えて選べない |
| 3 | 距離入力 | 0.0〜4.0を0.1刻みで入力でき、図と数値が一致する |
| 4 | 動画ログ登録 | 日付・タイトル・YouTube URLを保存し、動画一覧に表示される |
| 5 | URL制約 | YouTube以外のURLを登録できない |
| 6 | セットと動画の紐付け | 動画ログとセット範囲を保存し、最近の対戦に動画名・時間が表示される |
| 7 | JSON書出し／取込 | 書出したJSONを取込後、動画ログとセットが維持される |
| 8 | 保存持続 | 再読込後もIndexedDBの既存記録が表示される |
| 9 | 起動画面 | 日付順の動画カードが、全体ダッシュボードより先に表示される |
| 10 | 日別サマリー | 動画カードと動画詳細のセット数、勝敗、平均距離、主なダメージ源・被ダメージ源が紐付くセットから算出される |
| 11 | 動画詳細からのセット追加 | セット追加時に対象動画が自動選択され、保存後にその動画詳細へ反映される |

### 3.3 実機統合確認: 重要局面タイムライン（6件）

2026-09-11実装。要件定義書5.4に対応。

| # | テストケース | 確認内容 |
|---|---|---|
| 1 | 重要局面追加 | 1セットへ最大5件を保存できる |
| 2 | 時間形式 | `12:34`と`1:12:34`を受け付け、不正形式を拒否する |
| 3 | YouTube遷移 | タップ時に該当秒数から動画を開く |
| 4 | 未紐付け動画 | 遷移せず、動画登録を促す表示になる |
| 5 | JSON互換性 | 重要局面を含むJSONの書出し／取込で内容が維持される |
| 6 | 既存データ | 重要局面を持たない既存セットがエラーなく表示される |

### 3.4 JSONスキーマ互換性（2件）

| # | テストケース | 入力／前提 | 期待結果 |
|---|---|---|---|
| 1 | 旧JSON取込 | `milestones`を持たないスキーマ2のセット | 既存項目を維持し、空の重要局面として表示される |
| 2 | 新JSON取込 | 有効な重要局面を1〜5件持つスキーマ3のセット | 重要局面を含めて保存・書出しされる |

### 3.5 分析依頼導線（4件）

| # | テストケース | 確認内容 |
|---|---|---|
| 1 | 依頼文コピー | 動画URL、日付、タイトル、セット数、schemaVersion 3の指定を含む依頼文がコピーされる |
| 2 | 共有シート | Web Share API対応Androidで共有シートが開き、ユーザーが共有先を選べる |
| 3 | 共有非対応 | Web Share API非対応環境で、依頼文コピーへフォールバックする |
| 4 | JSON雛形 | 対象動画と紐付くセット、重要局面を含むスキーマ3 JSONが書出される |

## 4. 実機確認項目（ユーザー実施）

- [ ] GitHub Pages公開後、Android Chromeで更新版を再読込し、新しい表示が出る。
- [ ] PWAとしてインストール済みの場合も、更新後にアプリを再起動して表示を確認する。
- [ ] 日次の限定公開動画を1本登録し、動画を開く操作が想定どおり動く。
- [ ] JSONを書出して端末外に保管し、再取込できることを確認する。
- [ ] 重要局面実装後、実際の動画で該当秒数へ遷移し、時間のズレを確認する。
- [ ] 重要局面を追加・編集・削除し、再読込後も内容が維持される。
- [ ] 重要局面が5件のセットでは追加ボタンが無効になる。
- [ ] 動画詳細の「分析を依頼」から共有シートを開き、ChatGPTを選べるか確認する。
- [ ] 共有シートにChatGPTがない場合、「依頼文をコピー」を使って貼り付けられることを確認する。
